

  <title>Все пользователи</title>
 </head>
 <body>

<table class="table table-striped" border="1" >
   
<h1>Спиксок гостей</h1>
<p>Статус : <%= flash[:bad] %>  <%= flash[:good] %> <%=flash[:delet]%> </p>
 <tr>
        <th>id</th>
        <th>Имя</th>
   		<th>Фамилия</th>
   		<th>Удалить</th>
   		<th>Детально</th>
   		<th>Пришел</th>
   		
   		
   </tr>
<% @users.each do |person| %>
    
 
   <tr>
    	<td class="col1"><%= person.id %></td>
    	<td><%= link_to person.name, user_path(person) %></td>
    	<td><%= person.family %></td>
    	<td><%= link_to "Удалить", user_path(person), method: :delete, data: {confirm: "Точно удалять?"} %>	
  		</td>
    	<td><a href="http://localhost:3000/users/<%= person.id %>">Посмотреть Детально</a> </td>	
    	<td> <%= radio_button_tag(:age, "Пришел") %> </td>

	 </tr>
     
<% end %>
</table>

<br>
Добавь друга	
=begin
 №<form action="/users/" method="post">
   <p><input name="name" placeholder="Не менее 4 букв"> <input  name="family" placeholder="Любое значение"></p>
   <p><input type="submit" value="Добавить"></p>
   
  </form>
=end



<% @post_attachments.each do |p| %>
  <%= image_tag p.avatar_url, :style => "width: 300px; height: 250px;"  %>


  <h1>Listing post_attachments</h1>

<table>
  <thead>
    <tr>
      <th>Post</th>
      <th>Avatar</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @post_attachments.each do |post_attachment| %>
      <tr>
        <td><%= post_attachment.post_id %></td>
        <td><%= post_attachment.avatar %></td>
        <td><%= link_to 'Show', post_attachment %></td>
        <td><%= link_to 'Edit', edit_post_attachment_path(post_attachment) %></td>
        <td><%= link_to 'Destroy', post_attachment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post attachment', new_post_attachment_path %>
